# The engine itself needs numpy only (plus an optional open-source solver).
numpy>=1.26,<3

# Optional: exact mixed-integer lineup optimisation. Free and open source (CBC is bundled).
# Without it the engine falls back to its own branch-and-bound search and says so in the report.
pulp>=2.7

# Tests
pytest>=7.4

# Nothing here is needed for the engine to run; they are only used by optional data helpers
# you may add. Everything the repository ships works on the standard library plus numpy:
#   pandas       - convenient CSV wrangling in your own scripts
#   nba_api      - MIT-licensed NBA stats client (free, no key)
#   pybaseball   - MLB data helpers (free, no key)
